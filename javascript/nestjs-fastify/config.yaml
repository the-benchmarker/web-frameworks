framework:
  website: nestjs.com
  version: 8.0

  bootstrap:
    - npm -g install pm2
    - npm install
    - npm -g install @nestjs/cli
    - nest build

  command: pm2-runtime start dist/app.js -i $(nproc)
