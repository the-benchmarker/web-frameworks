
[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core"]

[project]
name='server'
version = '1.0.0'
description = "Simple benchmark implementation"
dependencies = ["index.py>=0.21,<0.22"]
