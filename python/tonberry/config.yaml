framework:
  github: Ayehavgunne/Tonberry
  version: 0.2

command: >
  gunicorn \
    --log-level warning  \
    --bind 0.0.0.0:8080  \
    --reuse-port  \
    --workers $(nproc)  \
    --worker-class uvicorn.workers.UvicornWorker  \
      server:app
