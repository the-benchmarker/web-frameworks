
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = 'benchmark'
version = '1.0.0'
description = "Simple benchmark implementation"
dependencies = ["starlette>=0.37,<0.38"]
[tool.hatch.build.targets.wheel]
packages = ["."]