framework:
  github: twisted/klein
  version: 23.5

  engines:
    - twisted

language:
  engines:
    twisted:
      command: PYTHONPATH=$(pwd) twistd -n web --class=server.application --port=tcp:3000
