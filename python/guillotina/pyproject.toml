
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = 'benchmark'
version = '1.0.0'
description = "Simple benchmark implementation"
dependencies = ["guillotina>=7,<7.1","zope.interface<6.0"]
[tool.hatch.build.targets.wheel]
packages = ["."]
