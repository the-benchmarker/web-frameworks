framework:
  website: aiohttp.readthedocs.io
  version: 3.7

command: >
  gunicorn  \
    --log-level warning  \
    --bind 0.0.0.0:8080  \
    --reuse-port  \
    --workers $(nproc)  \
    --worker-class aiohttp.GunicornUVLoopWebWorker \
      server:app
