
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = 'benchmark'
version = '1.0.0'
description = "Simple benchmark implementation"
dependencies = ["fastapi>=0.111,<0.112"]
