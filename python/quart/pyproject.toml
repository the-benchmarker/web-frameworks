
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = 'benchmark'
version = '1.0.0'
description = "Simple benchmark implementation"
dependencies = ["quart>=0.19,<0.20"]
[tool.hatch.build.targets.wheel]
packages = ["."]