
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = 'benchmark'
version = '1.0.0'
description = "Simple benchmark implementation"
dependencies = ["robyn>=0.54,<0.55"]
[tool.hatch.build.targets.wheel]
packages = ["."]
