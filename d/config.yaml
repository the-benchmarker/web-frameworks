language:
  version: "1.30"

  files:
    - "**/*.d"
    - dub.json
    - dub.selections.json
    - dub.sdl

  engines:
    default:
      command: /opt/web/server

framework:
  engines:
    - default
