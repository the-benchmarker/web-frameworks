language:
  version: 13.0

  files:
    - "**/*.cs"
    - web.csproj

  engines:
    default:
      command: dotnet /usr/src/app/out/web.dll
      environment:
        ASPNETCORE_URLS: http://*:3000

framework:
  engines:
    - default
