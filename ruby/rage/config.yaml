framework:
  github: rage-rb/rage
  version: 1.25

  engines:
    - iodine

  environment:
    RAGE_ENV: production

  build_deps:
    - libyaml-dev

language:
  engines:
    iodine:
      command: bundle exec rage s -p 3000 -b 0.0.0.0
