framework:
  github: ohler55/agoo
  version: 2.14

environment:
  AGOO_WORKER_COUNT: $(nproc)

command: /usr/src/app/bin/bundle exec /usr/bin/ruby /usr/src/app/app.rb

files:
  - app.rb
  - Gemfile

bootstrap:
  - bundle config set without "development test"
  - bundle config set path "vendor/bundle"
  - bundle install
  - bundle binstubs bundler
