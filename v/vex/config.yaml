framework:
  github: nedpals/vex
  version: 0.3.6

  bootstrap:
    - v install nedpals.vex
    - cd /opt/vlang && git fetch --all --tags && git checkout tags/weekly.2023.13 && make && v -version && cd /app

  build_flags:
    - -prod -cc clang