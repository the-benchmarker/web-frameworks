language:
  version: 24120e7a80d798a1625a08d5cc02b96e17148bdb
  files:
    - "**/*.v"
    - server
  bootstrap:
    - cd /opt/vlang && v -version && cd /app
  build_flags:

  engines:
    default:
      command: /app/server

framework:
  engines:
    - default
