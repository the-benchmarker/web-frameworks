main:
  depends_on:
    - crystal
    - ruby
    - nim
    - python
    - go
    - swift
    - rust
    - node
    - elixir
    - cpp
    - scala
    - csharp

crystal:
  depends_on:
    - router.cr
    - raze
    - kemal
    - lucky
    - amber
    - spider-gazelle

ruby:
  depends_on:
    - rails
    - sinatra
    - roda
    - rack-routing

nim:
  depends_on:
    - jester
    - mofuw

python:
  depends_on:
    - flask
    - japronto
    - django
    - sanic
    - tornado

go:
  depends_on:
    - iris
    - echo
    - gorilla_mux
    - fasthttprouter
    - gin

swift:
  depends_on:
    - vapor
    - perfect
    - kitura

rust:
  depends_on:
    - actix
    - iron
    - rocket
    - nickel

node:
  depends_on:
    - express
    - clusterexpress
    - polka
    - clusterpolka

elixir:
  depends_on:
    - plug
    - phoenix

cpp:
  depends_on:
    - evhtp

scala:
  depends_on:
    - akkahttp

csharp:
  depends_on:
    - aspnetcore

router.cr:
  command: |
    docker build -t router.cr . 1>&2
    docker run -td router.cr
  dir: crystal/router.cr

raze:
  command: |
    docker build -t raze . 1>&2
    docker run -td raze
  dir: crystal/raze

kemal:
  command: |
    docker build -t kemal . 1>&2
    docker run -td kemal
  dir: crystal/kemal

lucky:
  command: |
    docker build -t lucky . 1>&2
    docker run -td lucky
  dir: crystal/lucky

amber:
  command: |
    docker build -t amber . 1>&2
    docker run -td amber
  dir: crystal/amber

spider-gazelle:
  command: |
    docker build -t spider-gazelle . 1>&2
    docker run -td spider-gazelle
  dir: crystal/spider-gazelle

rails:
  command: |
    docker build -t rails . 1>&2
    docker run -td rails
  dir: ruby/rails

sinatra:
  command: |
    docker build -t sinatra . 1>&2
    docker run -td sinatra
  dir: ruby/sinatra

roda:
  command: |
    docker build -t roda . 1>&2
    docker run -td roda
  dir: ruby/roda

rack-routing:
  command: |
    docker build -t rack-routing . 1>&2
    docker run -td rack-routing
  dir: ruby/rack-routing

jester:
  command: |
    docker build -t jester . 1>&2
    docker run -td jester
  dir: nim/jester

mofuw:
  command: |
    docker build -t mofuw . 1>&2
    docker run -td mofuw
  dir: nim/mofuw

flask:
  command: |
    docker build -t flask . 1>&2
    docker run -td flask
  dir: python/flask

japronto:
  command: |
    docker build -t japronto . 1>&2
    docker run -td japronto
  dir: python/japronto

django:
  command: |
    docker build -t django . 1>&2
    docker run -td django
  dir: python/django

sanic:
  command: |
    docker build -t sanic . 1>&2
    docker run -td sanic
  dir: python/sanic

tornado:
  command: |
    docker build -t tornado . 1>&2
    docker run -td tornado
  dir: python/tornado

iris:
  command: |
    docker build -t iris . 1>&2
    docker run -td iris
  dir: go/iris

echo:
  command: |
    docker build -t echo . 1>&2
    docker run -td echo
  dir: go/echo

gorilla_mux:
  command: |
    docker build -t gorilla_mux . 1>&2
    docker run -td gorilla_mux
  dir: go/gorilla-mux

fasthttprouter:
  command: |
    docker build -t fasthttprouter . 1>&2
    docker run -td fasthttprouter
  dir: go/fasthttprouter

gin:
  command: |
    docker build -t gin . 1>&2
    docker run -td gin
  dir: go/gin

vapor:
  command: |
    docker build -t vapor . 1>&2
    docker run -td vapor
  dir: swift/vapor

perfect:
  command: |
    docker build -t perfect . 1>&2
    docker run -td perfect
  dir: swift/perfect

kitura:
  command: |
    docker build -t kitura . 1>&2
    docker run -td kitura
  dir: swift/kitura

actix:
  command: |
    docker build -t actix . 1>&2
    docker run -td actix
  dir: rust/actix

iron:
  command: |
    docker build -t iron . 1>&2
    docker run -td iron
  dir: rust/iron

rocket:
  command: |
    docker build -t rocket . 1>&2
    docker run -td rocket
  dir: rust/rocket

nickel:
  command: |
    docker build -t nickel . 1>&2
    docker run -td nickel
  dir: rust/nickel

express:
  command: |
    docker build -t express . 1>&2
    docker run -td express pm2-runtime app.js
  dir: node/express

clusterexpress:
  command: |
    docker build -t clusterexpress . 1>&2
    docker run -td clusterexpress pm2-runtime cluster_express.js
  dir: node/express

polka:
  command: |
    docker build -t polka . 1>&2
    docker run -td polka pm2-runtime app.js
  dir: node/polka

clusterpolka:
  command: |
    docker build -t clusterpolka . 1>&2
    docker run -td clusterpolka pm2-runtime cluster_polka.js
  dir: node/polka

plug:
  command: |
    docker build -t plug . 1>&2
    docker run -td plug
  dir: elixir/plug

phoenix:
  command: |
    docker build -t phoenix . 1>&2
    docker run -td phoenix
  dir: elixir/phoenix

evhtp:
  command: |
    docker build -t evhtp . 1>&2
    docker run -td evhtp
  dir: cpp/evhtp

akkahttp:
  command: |
    docker build -t akkahttp . 1>&2
    docker run -td akkahttp
  dir: scala/akkahttp

aspnetcore:
  command: |
    docker build -t aspnetcore . 1>&2
    docker run -td aspnetcore
  dir: csharp/aspnetcore
