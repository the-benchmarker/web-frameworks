# hello configuration

bind        0.0.0.0 8080
load        ./hello.so

domain * {
    static      /                   home
    static      /user               user
    dynamic     ^/user/([^/]+)$     user_details
}
