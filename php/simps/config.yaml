framework:
  website: simps.io
  version: 1.0

php_ext:
  - swoole

bootstrap:
  - composer install --no-dev --prefer-dist --classmap-authoritative
  - composer dumpautoload -o

command: /usr/bin/php /opt/web/sbin/simps.php http:start
