framework:
  github: amirfaramarzi/tower
  version: 2.0

build_deps:
  - libevent-dev
  - openssl-dev
  
php_mod:
  - pcntl
  - sockets

php_ext:
  - event
  - redis

before_command:
  - mkdir storage/logs -p

command: php server.php start
