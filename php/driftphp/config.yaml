framework:
  github: driftphp/driftphp
  version: 0.1

  files:
    - bin/server
    - Drift/config/**/*.yml

  engines:
    - reactphp

  command: php vendor/bin/server run 0.0.0.0:3000 --env=prod --quiet --workers=-1
