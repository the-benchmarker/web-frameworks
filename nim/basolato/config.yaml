framework:
  github: itsumura-h/nim-basolato
  version: 0.12.3

deps:
  - gcc
  - musl-dev
  - bsd-compat-headers
  - openssl
  - pcre
  - pcre-dev

build_opts: '-d:ssl --threads:on --threadAnalysis:off --gc:orc --putenv:PORT=3000'
