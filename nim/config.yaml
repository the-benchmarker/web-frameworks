language:
  version: "2.2"
  files:
    - config.nims
    - "**/*.nim"
    - "**/*.nimble"
    - "**/*.cfg"
  engines:
    default:
      command: /usr/src/app/server

framework:
  engines:
    - default
build_opts: "-d:release --opt:speed"
