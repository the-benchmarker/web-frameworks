# Package
version       = "0.1.0"
author        = "George Lemon"
description   = "A basic server based on PowPow library"
license       = "MIT"
bin           = @["server"]

# Dependencies

requires "nim >= 2.0.0"
requires "powpow  >= 0.1.2"