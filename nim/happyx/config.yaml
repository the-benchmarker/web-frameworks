framework:
  github: HapticX/happyx
  version: "1.0"

deps:
  - pcre-dev

build_opts: '--checks:off --assertions:off --warnings:off --hints:off -d:httpx --threads:on --mm:orc --define:usePcreHeader --passL: "/usr/lib/libpcre.a"'
build_with: nimble install -d -y
