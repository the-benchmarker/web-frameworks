framework:
  github: HapticX/happyx
  version: 0.15

deps:
  - pcre-dev

build_opts: '--define:danger --opt:speed --define:usePcreHeader --passL: "/usr/lib/libpcre.a"'
build_with: nimble install -d -y
