framework:
  website: activej.io
  version: 5.5

  build:
    - mvn clean package

  binaries:
    - target/benchmark.jar

language:
  default:
    engines:
      command: /usr/bin/java -jar target/benchmark.jar benchmark.App
