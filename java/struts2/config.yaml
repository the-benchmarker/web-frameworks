framework:
  website: struts.apache.org
  version: 7.0

  build:
    - mvn clean package

  binaries:
    - target/benchmark-7.0.0-standalone.jar

  command: java -jar target/benchmark-7.0.0-standalone.jar --port 3000
