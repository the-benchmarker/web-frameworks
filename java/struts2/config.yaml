framework:
  website: struts.apache.org
  version: 6.3

  build:
    - mvn clean package

  binaries:
    - target/benchmark-1.0.0-standalone.jar

  command: java -jar target/benchmark-1.0.0-standalone.jar --port 3000
