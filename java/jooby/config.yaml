framework:
  website: jooby.io
  version: 4.5

  build:
    - mvn clean package

  binaries:
    - target/benchmark.jar

language:
  engines:
    default:
      command: /usr/bin/java -jar target/benchmark.jar benchmark.App
