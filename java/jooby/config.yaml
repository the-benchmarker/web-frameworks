framework:
  website: jooby.io
  version: 2.8

build:
- mvn clean package

command: >
  /usr/bin/java -server
  -XX:+UseNUMA -XX:+UseParallelGC -XX:+AggressiveOpts -XX:-UseBiasedLocking -XX:+UseStringDeduplication
  -jar /opt/web/target/benchmark.jar benchmark.App
