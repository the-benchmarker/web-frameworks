framework:
  website: helidon.io
  version: 4.0

  build:
    - mvn clean package

  files:
    - target/helidon-mp.webserver.jar

  binaries:
    - target/helidon-mp.webserver.jar

  command: /usr/bin/java target/helidon-mp.webserver.jar
