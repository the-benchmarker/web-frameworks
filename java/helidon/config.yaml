framework:
  website: helidon.io
  version: 3.0.1

build:
  - mvn package

files:
  - target/helidon.webserver.jar

binaries:
  - target/helidon.webserver.jar

command: /usr/bin/java -XX:-RelaxAccessControlCheck -XX:+UseNUMA -XX:+UseParallelGC -jar /opt/web/target/helidon.webserver.jar
