framework:
  website: https://armeria.dev/
  version: 1.26.4

  build:
    - mvn package

  files:
    - target/benchmark.jar

  binaries:
    - target/benchmark.jar

  command: /usr/bin/java -jar /opt/web/target/benchmark.jar